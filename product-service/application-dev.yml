spring:
  application:
    name: product-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    datasource:
      platform: postgres
      url: jdbc:postgresql://localhost:5432/tesserys
      username: postgres
      password: postgres
server:
  port: 8010
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'